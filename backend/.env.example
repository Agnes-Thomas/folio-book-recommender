# Directory where GoodBooks CSVs are stored (auto-downloaded if missing)
DATA_DIR=goodbooks_data

# Number of ratings rows to use for SVD training (set to 0 for all ~6M)
SVD_SAMPLE=500000

# Server
PORT=8000
